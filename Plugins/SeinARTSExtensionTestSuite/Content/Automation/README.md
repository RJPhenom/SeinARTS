# Extension automation assets

Keep tiny Squad, Cover, CoverSquad, Movement+, and cross-extension test maps, Blueprints, and data
assets here. The base `SeinARTSTestSuite/Content` tree is reserved for framework-only fixtures so
the extension-stripped test profile never mounts assets whose classes are unavailable.
